/// Contracte per futures crides de xat al backend.
///
/// Encara no implementat: quan el servidor exposi converses, afegeix mètodes
/// tipus `fetchConversations` i fes servir `Chat.fromJson` des de
/// `lib/core/models/chat.dart`.
abstract class ChatsApi {
  // Future<List<Chat>> listConversations(int userId);
  // Future<void> sendMessage({required int chatId, required String text});
}
