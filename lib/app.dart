import 'package:flutter/material.dart';

class AgendatApp extends StatelessWidget {
  const AgendatApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(title: "Agenda't", home: Placeholder());
  }
}
